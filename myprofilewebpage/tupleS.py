# a tuple is exactly like a list only that is all item created are fixed (immutable) in this case round brackets () are used
daysOfaweek = ("Monday", "Tuesdays", "Wednesday" "Thursday", "Friday", "Saturday", "Sunday")
print (type(daysOfaweek))
print (len(daysOfaweek))